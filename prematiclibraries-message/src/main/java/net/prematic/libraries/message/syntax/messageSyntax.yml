

simple: 'Hallo %player%'


simpleCondition: '@hasRole(admin)?Hey admin:Hey user'
simpleCondition2: '(@hasRole(admin)?Hey admin:Hey user) that is cool'


loop: '@for(reason) %reason.id% -> %reason.name%'

loopAndCondition: |-
  'Reasons:'
  '@for(id,reason in reasons sort id) @if(hasPerm(reason.permission)) %reason.id% -> %reason.name%'
  'Use /ban <name>'



@for
@if
@else
@else-if
@ifNot
@else-ifNot

@CustomMethos


%placeHolder%